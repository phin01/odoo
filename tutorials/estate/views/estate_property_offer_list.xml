<?xml version="1.0"?>
<odoo>

    <record id="estate_property_offer_list_view" model="ir.ui.view">
        <field name="name">estate.property.offer.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list string="Offers" delete="false" editable="true" decoration-success="status == 'accepted'" decoration-danger="status == 'refused'">
                <field name="price" string="Price"/>
                <field name="status" string="Status"/>
                <field name="partner_id" string="Partner"/>
                <field name="validity" string="Validity"/>
                <field name="date_deadline" string="Deadline"/>
                <button name="action_accept_offer" type="object" icon="fa-check" invisible="status in ('refused', 'accepted')"/>
                <button name="action_refuse_offer" type="object" icon="fa-times" invisible="status in ('refused', 'accepted')"/>
            </list>
        </field>
    </record>

</odoo>